---
title: Embed a Form via iframe
description: Embed an Endatix form with embed.js, choose a height mode, and test it on a real cross-origin host.
sidebar_position: 2
---

import useBaseUrl from "@docusaurus/useBaseUrl";
import ThemedImage from "@theme/ThemedImage";
import playgroundLight from "./embed-playground_light.png";
import playgroundDark from "./embed-playground_dark.png";
import configureLight from "./embed-playground-configure_light.png";
import configureDark from "./embed-playground-configure_dark.png";
import eventsLight from "./embed-playground-events_light.png";
import eventsDark from "./embed-playground-events_dark.png";

# Embed a Form via iframe

Drop `embed.js` on any page. It inserts an iframe after the script tag (that is the form's position)
and keeps height in sync. Lifecycle events, payloads, and host-page listeners live in
[Embedded form events](/docs/developers/hub/embed-events) — this page is the snippet, height modes,
and how to test against a real host.

## The embed snippet

Copy it from the Hub share dialog, or write it by hand:

```html
<script
  src="https://YOUR_HUB_URL/embed/v1/embed.js"
  data-form-id="YOUR_FORM_ID"
></script>
```

`YOUR_HUB_URL` is the Hub origin that serves the form (e.g. `localhost:3000` locally).

| Attribute | Required | Meaning |
| --- | --- | --- |
| `data-form-id` | Yes | Published form id. |
| `data-height-mode` | No | `auto` (default) or `fill`. |
| `data-token` | No | Access token for a prefilled or editable submission. |
| `data-prefill` | No | Query fragment, e.g. `campaign=spring`. |

If both `data-token` and `data-prefill` are set, the token wins. Token creation:
[Form Prefilling and Sharing](/docs/guides/form-prefilling).

### Height modes

**`auto` (default).** Iframe grows and shrinks with the form. The page scrolls; the form does not.
Use this in normal document flow.

**`fill`.** Iframe takes the **container's** height — full-height panel, modal body, dedicated page.
The container needs a resolvable height (`height: auto` gives it nothing to fill). If the form is
taller than the container, make the container scroll.

```html
<div style="height: 100vh">
  <script
    src="https://YOUR_HUB_URL/embed/v1/embed.js"
    data-form-id="YOUR_FORM_ID"
    data-height-mode="fill"
  ></script>
</div>
```

## Test on a real host

`/embed/{formId}` is the **iframe document**, not a host page. Opening it skips `embed.js`, resize,
and host events. A real check needs a page on a **different origin** from Hub.

Local Development already has one: **embed playground**, `GET /dev/embed-host` on the API
(`https://localhost:5001` by default). Hub serves `embed.js` and the iframe; the API origin is the
host — same split as a customer site.

On in Development; `404` in Production unless you set:

```json
{
  "Endatix": {
    "DevTools": {
      "EmbedHost": {
        "Enabled": true,
        "HubBaseUrl": "http://localhost:3000"
      }
    }
  }
}
```

`HubBaseUrl` is the Hub origin for `embed.js` (falls back to `Endatix:Hub:HubBaseUrl`). Extra hosts
go in `Endatix:DevTools:EmbedHost:AllowedHubHosts`. Unknown `hubBaseUrl` query values return `400`.

### Query string is the contract

The URL fully describes the embed. **Configure** only GET-rewrites that query — nothing you cannot
type by hand. Agents and bookmarks should use the query, not the UI.

```
https://localhost:5001/dev/embed-host?formId=1442869143157080064&heightMode=fill
```

<ThemedImage
  alt="Embed playground with a form loaded in fill mode"
  sources={{
    light: useBaseUrl(playgroundLight),
    dark: useBaseUrl(playgroundDark),
  }}
/>

| Query | Meaning |
| --- | --- |
| `formId` | Form to embed. Omit for the empty builder shell. Required for `view=bare`. |
| `heightMode` | `fill`, or omit for `auto`. |
| `token` / `prefill` | Map to `data-token` / `data-prefill`. Token wins. |
| `hubBaseUrl` | Allowlisted Hub origin override. |
| `view` | `builder` (default) or `bare` (no chrome). |

Toolbar (builder): **Full / 768 / 375** preview width, **Reload**, **Copy snippet**, **Open in new
tab** (`view=bare` on the same query). **Log** and **Configure** fold the panels below.

<ThemedImage
  alt="Configure panel of the embed playground"
  sources={{
    light: useBaseUrl(configureLight),
    dark: useBaseUrl(configureDark),
  }}
/>

**Log** records the same `endatix:form-*` events your host page would receive. It stays collapsed,
badges unseen events, and opens on error. Payloads and how to listen:
[Embedded form events](/docs/developers/hub/embed-events).

<ThemedImage
  alt="Event log of the embed playground showing a form-loaded payload"
  sources={{
    light: useBaseUrl(eventsLight),
    dark: useBaseUrl(eventsDark),
  }}
/>

### `view=bare`

No toolbar, log, or configure — for e2e and screenshots:

```
https://localhost:5001/dev/embed-host?formId=1442869143157080064&view=bare
```

Invalid or missing `formId` → `400`.

:::warning
DevTools only — not a production embed. Keep it disabled outside development.
:::

## Troubleshooting

**Nothing renders.** Console: blocked script or mixed content. HTTPS host cannot load HTTP
`embed.js`. Confirm `data-form-id` is a published form.

**`fill` collapses.** Give the parent an explicit height. See Height modes above.

**Resize or events.** Host CSS, hidden parents, listener timing, CSP —
[Embedded form events → Troubleshooting](/docs/developers/hub/embed-events#troubleshooting).
