---
sidebar_position: 2
title: System Requirements
description: What Endatix needs before you install — .NET 10 for the API, Node.js 20.9+ for the Hub, SQL Server 2025+ or PostgreSQL with the pg_trgm extension, and supported browsers.
---

import Link from "@docusaurus/Link";

# System Requirements

Endatix ships as two deployable parts: the **Endatix API** (.NET) and the **Endatix Hub** (Next.js). Run the API on its own for headless, API-only use — the Hub always needs a reachable API. Both run on **macOS, Windows (including WSL), and Linux**.

<div className="edx-grid">
  <div className="card edx-card">
    <div className="edx-card__head">
      <Icon name="server" />
      <div>
        <p className="edx-card__eyebrow">Backend</p>
        <h3 className="edx-card__title">Endatix API</h3>
      </div>
    </div>
    <dl className="edx-specs">
      <dt>SDK</dt>
      <dd><a href="https://dotnet.microsoft.com/download/dotnet/10.0">.NET 10</a> — <code>10.0.100</code> or newer</dd>
      <dt>Runtime</dt>
      <dd>ASP.NET Core 10 (<code>10.0.6</code>+) for published apps</dd>
      <dt>Database</dt>
      <dd>SQL Server or PostgreSQL — <a href="#databases">see below</a></dd>
      <dt>Dev URL</dt>
      <dd><code>{"https://localhost:5001"}</code></dd>
    </dl>
    <div className="edx-card__footer">Ships as NuGet packages you host inside your own .NET app, or as the <code>endatix-api</code> container image.</div>
  </div>
  <div className="card edx-card">
    <div className="edx-card__head">
      <Icon name="dashboard" accent="violet" />
      <div>
        <p className="edx-card__eyebrow">Frontend</p>
        <h3 className="edx-card__title">Endatix Hub</h3>
      </div>
    </div>
    <dl className="edx-specs">
      <dt>Node.js</dt>
      <dd><a href="https://nodejs.org/">20.9 or newer</a> (tested up to 25.0)</dd>
      <dt>pnpm</dt>
      <dd><a href="https://pnpm.io/installation">9 or newer</a> (tested up to 10.33)</dd>
      <dt>API</dt>
      <dd>A reachable Endatix API — <Link to="/docs/developers/hub/environment"><code>ENDATIX_BASE_URL</code></Link></dd>
      <dt>Dev URL</dt>
      <dd><code>{"http://localhost:3000"}</code></dd>
    </dl>
    <div className="edx-card__footer">Node and pnpm are for building from source. The prebuilt <code>endatix-hub</code> image needs Docker, not Node.</div>
  </div>
</div>

## Check what you have

```bash
dotnet --list-sdks   # need 10.0.100 or newer
node --version       # need v20.9.0 or newer
pnpm --version       # need 9.0.0 or newer
docker --version     # optional — for Compose self-hosting
```

## Databases

Pick **one** provider per deployment and set it in [Persistence Settings](/docs/configuration/settings/persistence-settings). Endatix stores form and submission payloads in **native JSON column types**, so the provider *version* matters — not just the vendor.

<div className="edx-grid">
  <div className="card edx-card">
    <div className="edx-card__head">
      <Icon name="database" />
      <div>
        <p className="edx-card__eyebrow">Provider</p>
        <h3 className="edx-card__title">SQL Server</h3>
      </div>
    </div>
    <dl className="edx-specs">
      <dt>Version</dt>
      <dd><strong>2025 or later</strong>, or Azure SQL Database</dd>
      <dt>Setup</dt>
      <dd>No extensions required</dd>
      <dt>Watch out</dt>
      <dd>Migrations use the native <code>json</code> type — <a href="#sql-server-2025-or-later">why</a></dd>
    </dl>
  </div>
  <div className="card edx-card">
    <div className="edx-card__head">
      <Icon name="database" />
      <div>
        <p className="edx-card__eyebrow">Provider</p>
        <h3 className="edx-card__title">PostgreSQL</h3>
      </div>
    </div>
    <dl className="edx-specs">
      <dt>Version</dt>
      <dd>Any supported release — the sample stack runs 17.6</dd>
      <dt>Setup</dt>
      <dd><span className="edx-pill edx-pill--required">Required</span> <code>pg_trgm</code> extension</dd>
      <dt>Watch out</dt>
      <dd>Payloads are <code>jsonb</code> — <Link to="/docs/guides/json-to-jsonb-migration">migration guide</Link></dd>
    </dl>
  </div>
</div>

### SQL Server 2025 or later

Migrations declare payload columns as `json`, a type added in SQL Server 2025. On 2022 and earlier they fail with `Cannot find data type json`. Azure SQL Database supports the type.

### PostgreSQL `pg_trgm` extension

Data-list label search uses trigram indexes, and the migration that creates them fails without `pg_trgm`. Create it before you migrate:

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
```

Managed hosts may need the extension allowlisted first — on Azure Database for PostgreSQL that is the `azure.extensions` server parameter, which [Azure Deployment](/docs/building-your-solution/deployment/azure) covers and the sample Bicep sets for you.

## Browsers

The Hub — including public form-filling pages — targets the Next.js 16 baseline:

- **Chrome** 111+
- **Edge** 111+
- **Firefox** 111+
- **Safari** 16.4+

Older browsers load the page but may fail on modern JavaScript syntax — see [Next.js supported browsers](https://nextjs.org/docs/architecture/supported-browsers).

## Optional tooling

Needed only for the path you take:

- **Docker Desktop** (Compose v2) — [self-hosting with Compose](/docs/building-your-solution/deployment/self-hosting)
- **Kubernetes + Helm 3** — cluster self-hosting, same images and database rules; Hub is configured with [request-time `ENDATIX_*` env](/docs/developers/hub/environment)
- **Azure CLI / SWA CLI** — [Azure Deployment](/docs/building-your-solution/deployment/azure)
- **`dotnet-ef`** — running migrations by hand instead of at startup ([Persistence Settings](/docs/configuration/settings/persistence-settings))
- **Git** — [installing from the repository](/docs/getting-started/setup-repository)

## Next steps

<div className="edx-grid edx-grid--compact">
  <Link className="card edx-linkcard" to="/docs/getting-started/quick-start">
    <Icon name="rocket" />
    <span className="edx-linkcard__title">Quick Start</span>
    <span className="edx-linkcard__desc">Compare the install paths and pick one</span>
  </Link>
  <Link className="card edx-linkcard" to="/docs/getting-started/setup-nuget-package">
    <Icon name="package" />
    <span className="edx-linkcard__title">NuGet setup</span>
    <span className="edx-linkcard__desc">Add Endatix to an existing .NET app</span>
  </Link>
  <Link className="card edx-linkcard" to="/docs/building-your-solution/deployment/self-hosting">
    <Icon name="boxes" />
    <span className="edx-linkcard__title">Self-hosting</span>
    <span className="edx-linkcard__desc">Run API, Hub, and a database with Compose</span>
  </Link>
</div>
