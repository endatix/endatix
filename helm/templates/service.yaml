apiVersion: v1
kind: Service
metadata:
  name: {{ include "endatix-api.fullname" . }}
  labels:
    {{- include "endatix-api.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "endatix-api.selectorLabels" . | nindent 4 }}
  ports:
  - port: 80
    targetPort: 8080
