# FeatureFlows

Place **domain journey** integration tests here (multi-step flows for forms, submissions, tenants, etc.).

Use `[Trait("Category", "FeatureFlow")]` on new test classes. See repository root `tests/README.md` for taxonomy and naming.
